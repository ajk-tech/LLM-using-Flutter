name: minister_ai_chief_of_staff_demo
description: Frontend-only Flutter prototype for an on-premise AI assistant demo.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
